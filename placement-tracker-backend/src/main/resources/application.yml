spring:
  application:
    name: placement-tracker-backend
  datasource:
    url: jdbc:mysql://placement-mysql:3306cd /placement_tracker
    username: root
    password: Rohith4579*
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

server:
  port: 8080
  servlet:
    context-path: /api
  error:
    include-message: always

logging:
  level:
    root: INFO
    '[com.placementtracker]': DEBUG
